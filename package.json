{
  "name": "@markis/stattleship",
  "description": "Node API for stattleship (https://www.stattleship.com/)",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "build": "npx tsc && npm run build:script && npm run build:cleanup",
    "build:script": "npx rollup -c",
    "build:cleanup": "rm -rf ./tmp/"
  },
  "devDependencies": {
    "@types/node": "^8.0.25",
    "dts-bundle": "^0.7.3",
    "rollup": "^0.49.2",
    "rollup-plugin-typescript": "^0.8.1",
    "tslint": "^5.6.0",
    "typescript": "^2.4.2"
  }
}
